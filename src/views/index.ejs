<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">

    <!-- fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap" rel="stylesheet">

    <title>Par O No</title>
</head>
<body>
    
    <main class="main-content">
        <%if(number.length > 0){%>
            <span class="selected-number"><%=number%></span>
        <%}%>

        <form action="/verify" method="POST" class="form">
            <input type="number" name="val" placeholder="Ingrese un numero par" autocomplete="off">
            <button>Resultado</button>
        </form>

        <% if(message.length > 0){ %>
            <span class="result"><%=message%></span>
        <%}else{%>
            <span class="result">Resultado</span>
        <%}%>
        

    </main>

    <script src="main.js"></script>

</body>
</html>